(async()=>{'use strict';const root=document.getElementById('leaderboardRows'),notice=document.getElementById('leaderboardNotice');const fmt=v=>new Intl.NumberFormat('fr-FR').format(Number(v)||0);try{const data=await ByeBaillyAPI.request('/leaderboard?limit=100');if(!data.items.length){notice.textContent='Le classement est encore vide.';return;}for(const item of data.items){const row=document.createElement('div');row.className='leaderboard-row';const current=Math.max(0,...Object.values(item.stats?.streaks||{}).map(s=>Number(s?.actuel)||0));row.innerHTML=`<span class="leaderboard-rank">#${item.rank}</span><a class="leaderboard-user" href="profil.html?user=${encodeURIComponent(item.username)}"></a><span data-label="Correctes">${fmt(item.correct)}</span><span data-label="Réussite">${(Number(item.successRate||0)*100).toLocaleString('fr-FR',{maximumFractionDigits:1})} %</span><span data-label="Tentatives">${fmt(item.attempts)}</span><span data-label="Série">${fmt(current)}</span>`;row.querySelector('.leaderboard-user').textContent=item.username;root.append(row);}}catch(err){notice.textContent=navigator.onLine?`Classement indisponible : ${err.message}`:'Le classement nécessite une connexion Internet.';}})();
